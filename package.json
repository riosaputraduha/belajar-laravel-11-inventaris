{
    "private": true,
    "type": "module",
    "scripts": {
        "build": "composer install --no-dev && php artisan optimize && vite build",
        "dev": "vite",
        "start": "echo \"No start script needed; served by Vercel PHP.\""
    },
    "devDependencies": {
        "autoprefixer": "^10.4.20",
        "axios": "^1.7.4",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^1.0",
        "postcss": "^8.4.47",
        "tailwindcss": "^3.4.13",
        "vite": "^6.0"
    },
    "engines": {
        "node": "18.x"
    }
}
